const RouteModel = {
    index : {
        getLink : () => "/",
        getPath : () => "/"
    },
    aboutUs : {
        getLink : () => "/about-us",
        getPath : () => "/about-us",
    },
    portfolio : {
        getLink : () => "/portfolio",
        getPath : () => "/portfolio",
    }
}
export default RouteModel;