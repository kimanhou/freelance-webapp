const RouteModel = {
    index : {
        getLink : () => "/",
        getPath : () => "/"
    },
    aboutUs : {
        getLink : () => "/about-us",
        getPath : () => "/about-us",
    }
}
export default RouteModel;